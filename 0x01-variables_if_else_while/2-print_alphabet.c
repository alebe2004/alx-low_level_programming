#include <stdio.h>

/**
 * main - main function
 *Description: Printing alphabets inlowercase 
 * Return: Returen
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
